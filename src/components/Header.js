import '../styles/Header.styles.css'

export default function Header(){
    return (
        <div className="header">
            <h2>
                React Table (posts from jsonplaceholder.com)
            </h2>
        </div>
    )
}